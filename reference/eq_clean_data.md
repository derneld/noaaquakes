# Clean Earthquake Data

Processes raw NOAA data into a tidy format suitable for visualization.

## Usage

``` r
eq_clean_data(df)
```

## Arguments

- df:

  A data frame containing raw NOAA earthquake data.

## Value

A cleaned data frame with converted types, a Date column, and separated
Location/State columns.
